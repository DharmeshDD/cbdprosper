<div class="service" style="background-image: url('{{ section.settings.background_image | img_url:'master' }}');">
    <div class="container">
    <div class="heading">
       {% if section.settings.heading != blank %}
          <h2 class="text-center">{{ section.settings.heading }}</h2>
       {% endif %}
    </div>
    <div class="services">
       {% if section.blocks != blank %}
          <div class="row">
             {% for block in section.blocks %}
                <div class="col-lg-6">
                   {% if block.settings.logo-image != blank  %}
                      <div class="service-logo text-center"> 
                          <img src="{{ block.settings.logo-image | img_url:'master' }}" alt="{{ block.settings.heading }}">
                     </div>
                   {% endif %}
                   <div>
                      {% if block.settings.heading != blank  %}
                         <h2 class="text-center">{{ block.settings.heading }}</h2>
                      {% endif %}
                   </div>
                   <div>
                      {% if block.settings.button_label != blank  %}
                         <a href="{{ block.settings.button_link }}" class="btn btn-transparent text-center">{{ block.settings.button_label }}</a>
                      {% endif %}
                   </div>
                   <div> 
                      {% if block.settings.text != blank  %}
                         <p class="text-center">{{ block.settings.text }}</p>
                      {% endif %}
                   </div>
                </div>
             {% endfor %}
          </div>
       {% endif %}
    </div>
    </div>
 </div>
 {% schema %}
 {
    "name":"Services",
    "settings":[
       {
          "id":"background_image",
          "type":"image_picker",
          "label":"Background image"
       },
       {
          "type":"text",
          "id":"heading",
          "label":"Heading"
       }
    ],
    "blocks":[
       {
          "type":"image-section",
          "name":"service",
          "settings":[
             {
                "id":"logo-image",
                "type":"image_picker",
                "label":"Image"
             },
             {
                "id":"heading",
                "type":"text",
                "label":"Heading"
             },
             {
                "id":"button_label",
                "type":"text",
                "label":"Button label"
             },
             {
                "id":"button_link",
                "type":"url",
                "label":"Button link"
             },
             {
                "id":"text",
                "type":"html",
                "label":"Text"
             }
          ]
       }
       
    ],
    "presets":[
       {
          "name":"Services",
          "category":"Services"
       }
    ]
 }
 {% endschema %}
 